// Next keys templates by route segment. Remount only page content below the
// persistent Layout; no wrapper, client boundary or navigation interception.
// Operational descendants have no route-motion opt-in and remain immediate.
export default function Template({ children }) {
  return children
}
